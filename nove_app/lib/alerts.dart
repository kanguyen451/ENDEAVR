import 'package:flutter/material.dart';

class Alerts extends StatelessWidget {
  Widget build(BuildContext context) {
    return new Scaffold(
        appBar: AppBar(
          title: Text("Alerts"),
          backgroundColor: Color(0xff2389FF),
        ),
        body: Center(
          child: Text("Alerts page under construction"),
        ));
  }
}
